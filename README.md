# VMWare Terraform Code Generator

This is a simple tool to generate Terraform code for VMWare vSphere from a list of VMs in a CSV file.

# Usage

```
python3 main.py -i <input_file> -o <output_file>
```
